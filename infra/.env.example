# Variables used by infra/docker.compose.yml for compose-time substitution.
# If you want to share the root .env instead, run:
#   docker compose --env-file ../.env up -d
# and ignore this file.

# Database
DB_USER=docsense
DB_PASSWORD=docsense123
DB_NAME=docsense

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123